# 明日开工提示词：旧生图 Skill 拉取与升级审计

请继续处理“旧生图 Skill”的拉取与升级。今天已经完成并发布内容核心版 `su-webstyle-top12` v0.2.5，明天不要重新设计核心版，从远端现状继续。

## 已知源

- 内容核心版仓库：`https://github.com/doublesq97-ui/su-webstyle-top12`
- Core 12 在线演示：`https://doublesq97-ui.github.io/su-webstyle-top12/`
- 旧生图 Skill 仓库或本地路径：先从已登录 GitHub 的私有与公开仓库中定位；若仍有多个候选，只问我一次，让我确认准确仓库。

## 目标

先把旧生图 Skill 安全拉到最新，判断它是否需要同步内容核心版 v0.2.5 的新规则，再决定 keep / merge / replace。重点不是把网页 Skill 原样复制过去，而是把可复用的风格判断、Auto 路由、蓝红 signal 规则、12 个 recipe 与 Style Evidence 转译成真正适合图片生成的提示词编译流程。

## 执行顺序

1. 读取全局与目标仓库的 `AGENTS.md`、`SKILL.md` 和项目文档。
2. 检查 `git status`、当前分支、远端与本地差异。禁止覆盖未提交改动；工作区脏时先说明影响，再采用可逆方式同步。
3. 拉取远端最新内容，记录当前版本与最后提交。
4. 对照内容核心版 v0.2.5，审计旧生图 Skill 的触发条件、输入字段、风格目录、Auto 选择、提示词结构、图片比例、文字处理、负面约束、Evidence 与验证方式。
5. 先给我一份紧凑中文结论：用户问题、触发方式、核心路径、预期输出、与现有 Skills 的关系、keep / merge / replace 建议，以及为什么它应继续作为 Skill，而不是普通 prompt 或模板。
6. 若确认需要更新，直接实现最小且完整的修改：保留生图 Skill 的职责边界，使用 Codex 内置 `image_gen` 作为默认最终位图路线，不用 HTML、SVG、PIL 或截图替代成品。
7. 为至少 3 个代表性场景准备可复用最终提示词：AI 产品封面、文化 / 海报主题、结构化信息图。每个示例必须展示风格路由理由、主 signal / flash 规则、构图、文字、比例和禁止项。
8. 运行 Skill 校验与样例 dry-run，提供文件引用和验证结果。不要只写方案文档。
9. 完成后再提交并推送到它自己的远端；这是我们的仓库，默认直接推当前主分支，不创建 PR，除非我另行要求。

## 质量边界

- 内容核心版是风格判断与 recipe 的上游来源；生图 Skill 是图片提示词与最终位图执行层，两者不要合并成一个职责混乱的 Skill。
- 同一张图只允许一个主信号色。Restrained Swiss 自动路由：AI / 产品 / 数字工具默认蓝 `#0B3DFF`；文化 / 海报 / 建筑语义优先红 `#E30613`；黄色 `#FFE600` 只做 tiny flash。
- 不编造品牌、指标、人物身份、版权素材、Logo 或二维码。
- 中文文字必须短、准确、可读；若模型排字错误，继续通过图片模型修订，不默认切换到代码叠字。
- 今天不要生成图片；明天使用本提示词开工后再执行拉取、审计、修改与验证。
